# i18n Refactor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一第三方 locale 同步入口到 `LangEffect.onLocaleChange`，删除 admin-i18n 包和 admin app 中的死代码与噪音层。

**Architecture:** `setupI18n` 只负责初始化；`LangEffect` 是所有响应式副作用（dayjs 等）的唯一触发点；`locales/sync.ts` 集中存放第三方同步逻辑。

**Tech Stack:** React, i18next, react-i18next, Jotai, dayjs, TypeScript, pnpm workspace

**Spec:** `docs/superpowers/specs/2026-05-28-i18n-refactor-design.md`

---

## File Map

| 操作 | 文件 |
|------|------|
| 修改 | `packages/web/admin-i18n/src/index.ts` |
| 修改 | `packages/web/admin-i18n/src/features/lang/LangEffect.tsx` |
| 修改 | `packages/web/admin-i18n/src/features/lang/LangSwitch.tsx` |
| 删除 | `packages/web/admin-i18n/src/config/i18n.ts` |
| 删除 | `packages/web/admin-i18n/src/features/lang/use-lang.ts` |
| 新建 | `apps/admin/src/locales/sync.ts` |
| 删除 | `apps/admin/src/locales/dayjs.ts` |
| 删除 | `apps/admin/src/locales/locale.ts` |
| 修改 | `apps/admin/src/locales/index.ts` |
| 修改 | `apps/admin/src/plugins/dayjs.ts` |
| 修改 | `apps/admin/src/features/effects/GlobalEffect.tsx` |

---

## Task 1: 清理 admin-i18n 包噪音层

**Files:**
- Modify: `packages/web/admin-i18n/src/features/lang/LangEffect.tsx`
- Modify: `packages/web/admin-i18n/src/features/lang/LangSwitch.tsx`
- Modify: `packages/web/admin-i18n/src/index.ts`
- Delete: `packages/web/admin-i18n/src/config/i18n.ts`
- Delete: `packages/web/admin-i18n/src/features/lang/use-lang.ts`

- [ ] **Step 1: 更新 LangEffect.tsx 的 import 路径**

将 `packages/web/admin-i18n/src/features/lang/LangEffect.tsx` 中第 5 行改为：

```tsx
import { useEffect } from 'react';

import type { LangType } from '../../types';

import { useLang } from '../../hooks/use-lang';

interface LangEffectProps {
  /** Extra effect that should run after the active admin locale changes. */
  onLocaleChange?: (locale: LangType) => void;
}

const LangEffect = (props: LangEffectProps) => {
  const { onLocaleChange } = props;

  const { locale } = useLang();

  useEffect(() => {
    document.documentElement.lang = locale;
    onLocaleChange?.(locale);
  }, [locale, onLocaleChange]);

  return null;
};

export default LangEffect;
```

- [ ] **Step 2: 更新 LangSwitch.tsx 的 import 路径**

将 `packages/web/admin-i18n/src/features/lang/LangSwitch.tsx` 中第 6 行：

```ts
import { useLang } from './use-lang';
```

改为：

```ts
import { useLang } from '../../hooks/use-lang';
```

- [ ] **Step 3: 更新 index.ts — 删除 config/i18n 中间层引用，移除 locales 导出**

将 `packages/web/admin-i18n/src/index.ts` 完整替换为：

```ts
// Atoms
export { currentLangOptionAtom, fallbackLangAtom, langAtom, localeOptionsAtom } from './atoms/lang';
// Config
export { defaultLangConfig } from './config/default';
export { default as LangEffect } from './features/lang/LangEffect';

export { default as LangSwitch } from './features/lang/LangSwitch';

// Hooks
export { useLang } from './hooks/use-lang';
export type { UseLangReturn } from './hooks/use-lang';

// Runtime
export { $t, getCurrentLang, i18n, loadLocaleMessages, reactI18nextInstance, setLng, setupI18n } from './i18n';

// Types
export type {
  LangConfig,
  LangOption,
  LangType,
  LocaleChangeHandler,
  LocaleSetupOptions,
  LocaleStorage
} from './types';

// Utils
export { getLangFromStorage, getLangLabel, saveLangToStorage } from './utils/helpers';
```

注意：移除了 `initI18n`（`setupI18n` 的别名）、`adminLocales` / `locales` 导出。如果外部有消费 `initI18n`，改用 `setupI18n`；如果有消费 `adminLocales`，改用直接 import。

- [ ] **Step 4: 删除 config/i18n.ts**

```bash
rm packages/web/admin-i18n/src/config/i18n.ts
```

- [ ] **Step 5: 删除 features/lang/use-lang.ts**

```bash
rm packages/web/admin-i18n/src/features/lang/use-lang.ts
```

- [ ] **Step 6: 在 admin-i18n 包目录下运行 typecheck**

```bash
cd packages/web/admin-i18n && pnpm typecheck
```

期望输出：无错误（exit 0）

- [ ] **Step 7: Commit**

```bash
git add packages/web/admin-i18n/src/
git commit -m "refactor(admin-i18n): remove re-export noise layers in config/i18n and features/lang/use-lang"
```

---

## Task 2: 创建 locales/sync.ts，删除死文件

**Files:**
- Create: `apps/admin/src/locales/sync.ts`
- Delete: `apps/admin/src/locales/dayjs.ts`
- Delete: `apps/admin/src/locales/locale.ts`

- [ ] **Step 1: 创建 apps/admin/src/locales/sync.ts**

```ts
// oxlint-disable import/no-unassigned-import
import { locale } from 'dayjs';

import 'dayjs/locale/zh-cn';
import 'dayjs/locale/en';

const localMap: Record<I18n.LangType, string> = {
  'zh-CN': 'zh-cn',
  'en-US': 'en'
};

export function syncLocales(lang: I18n.LangType) {
  locale(localMap[lang]);
}
```

- [ ] **Step 2: 删除 dayjs.ts 和 locale.ts**

```bash
rm apps/admin/src/locales/dayjs.ts
rm apps/admin/src/locales/locale.ts
```

- [ ] **Step 3: Commit**

```bash
git add apps/admin/src/locales/sync.ts
git rm apps/admin/src/locales/dayjs.ts apps/admin/src/locales/locale.ts
git commit -m "refactor(admin): replace dayjs.ts with sync.ts, delete unused locale.ts"
```

---

## Task 3: 简化 locales/index.ts

**Files:**
- Modify: `apps/admin/src/locales/index.ts`

- [ ] **Step 1: 用简化版本覆盖 locales/index.ts**

将 `apps/admin/src/locales/index.ts` 完整替换为：

```ts
import { setupI18n as setupCoreI18n } from '@skyroc/web-admin-i18n';
import type { LocaleSetupOptions } from '@skyroc/web-admin-i18n';

import { globalConfig } from '@/config';
import { localStg } from '@/utils/storage';

export async function setupI18n(options: LocaleSetupOptions<I18n.LangType> = {}) {
  await setupCoreI18n({
    defaultLocale: globalConfig.defaultLang,
    fallbackLocale: 'en-US',
    localeOptions: globalConfig.defaultLangOptions,
    missingWarn: import.meta.env.DEV,
    storage: {
      getLocale: () => localStg.get('lang'),
      setLocale: lang => localStg.set('lang', lang)
    },
    ...options
  });
}

export { $t } from '@skyroc/web-admin-i18n';
```

- [ ] **Step 2: Commit**

```bash
git add apps/admin/src/locales/index.ts
git commit -m "refactor(admin): simplify locales/index.ts, remove dead exports and onLocaleChange"
```

---

## Task 4: 移除 plugins/dayjs.ts 中冗余的 syncLocale

**Files:**
- Modify: `apps/admin/src/plugins/dayjs.ts`

- [ ] **Step 1: 更新 plugins/dayjs.ts**

将 `apps/admin/src/plugins/dayjs.ts` 完整替换为：

```ts
import type { SetupDayjsOptions } from '@skyroc/web-admin-runtime';

export const adminDayjsPluginOptions: SetupDayjsOptions = {
  withLocaleData: true
};
```

`syncLocale` 字段移除：它只在插件初始化时调用一次且无参数（始终使用默认语言 `zh-CN`），`LangEffect.onLocaleChange` mount 时会以正确语言覆盖它，因此是冗余的。

- [ ] **Step 2: Commit**

```bash
git add apps/admin/src/plugins/dayjs.ts
git commit -m "refactor(admin): remove redundant syncLocale from dayjs plugin options"
```

---

## Task 5: 更新 GlobalEffect.tsx — 接入 syncLocales

**Files:**
- Modify: `apps/admin/src/features/effects/GlobalEffect.tsx`

- [ ] **Step 1: 更新 GlobalEffect.tsx**

将 `apps/admin/src/features/effects/GlobalEffect.tsx` 完整替换为：

```tsx
import { LangEffect } from '@skyroc/web-admin-i18n';
import { ThemeEffect } from '@skyroc/web-admin-theme';

import { syncLocales } from '@/locales/sync';

const GlobalEffect = () => {
  return (
    <>
      <ThemeEffect />
      <LangEffect onLocaleChange={syncLocales} />
    </>
  );
};

export default GlobalEffect;
```

- [ ] **Step 2: Commit**

```bash
git add apps/admin/src/features/effects/GlobalEffect.tsx
git commit -m "feat(admin): wire syncLocales into LangEffect.onLocaleChange"
```

---

## Task 6: 全量 typecheck + 手动验收

**Files:** 只读验证，不修改代码

- [ ] **Step 1: 在 admin app 目录运行 typecheck**

```bash
cd apps/admin && pnpm typecheck
```

期望输出：无错误（exit 0）。若有错误，根据错误信息定位并修复，常见原因：
- 某处还在导入已删除的 `setDayjsLocale`（改为从 `@/locales/sync` 导入 `syncLocales`）
- 某处还在导入 `initI18n`（改为 `setupI18n`）

- [ ] **Step 2: 启动开发服务器，手动验证**

```bash
cd apps/admin && pnpm dev
```

验证清单：
1. 页面正常加载，无控制台报错
2. 点击语言切换按钮（右上角 language icon）→ 切换到英文
3. 确认以下三点同步更新：
   - antd 组件语言变化（如日期选择器显示英文）
   - dayjs 格式化随语言变化（如首页图表日期格式）
   - `document.documentElement.lang` 属性变化（DevTools → Elements → `<html lang="en-US">`）
4. 刷新页面后语言偏好保持（localStorage `lang` key 正确存储）

- [ ] **Step 3: 最终 Commit（如有遗漏修复）**

若 Step 1 有修复：
```bash
git add -p
git commit -m "fix(admin): resolve type errors after i18n refactor"
```
