import { tv } from 'tailwind-variants';

export const backTopVariants = tv({
  slots: {
    icon: 'h-5 w-3 -translate-y-0.5 rotate-180 border-b-[2.5px] border-r-[2.5px] border-primary-foreground'
  }
});
